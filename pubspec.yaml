name: fl_studio
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  smooth_page_indicator: ^1.0.0+2
  internet_connection_checker: ^1.0.0+1
  connectivity_plus: ^3.0.2
  flutter_secure_storage: ^8.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  generate: true

  assets:
    - lib/assets/
    - lib/assets/part1/img/
    - lib/assets/part1/